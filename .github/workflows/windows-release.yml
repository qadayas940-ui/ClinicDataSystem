name: Windows installer

on:
  workflow_dispatch:
  pull_request:
    branches: ["develop"]
  push:
    branches: ["develop"]
    tags: ["v*"]

permissions:
  contents: write

concurrency:
  group: windows-installer-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            requirements.txt
            requirements-dev.txt
            requirements-build.txt

      - name: Install dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-build.txt -r requirements-dev.txt

      - name: Resolve package version
        shell: pwsh
        run: |
          if ($env:GITHUB_REF -like "refs/tags/v*") {
            $version = $env:GITHUB_REF_NAME.Substring(1)
          } else {
            $version = "1.0.0-dev"
          }
          "APP_VERSION=$version" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          Write-Host "Package version: $version"

      - name: Django system check
        env:
          DJANGO_SETTINGS_MODULE: config.settings.testing
        run: python manage.py check

      - name: Verify migrations are committed
        env:
          DJANGO_SETTINGS_MODULE: config.settings.testing
        run: python manage.py makemigrations --check --dry-run

      - name: Run automated tests
        env:
          DJANGO_SETTINGS_MODULE: config.settings.testing
        run: python manage.py test

      - name: Lint Python source
        run: python -m ruff check .

      - name: Build desktop bundle
        run: pyinstaller --clean --noconfirm ClinicDataSystem.spec

      - name: Verify desktop executable exists
        shell: pwsh
        run: |
          $exe = "dist\ClinicDataSystem\ClinicDataSystem.exe"
          if (-not (Test-Path $exe)) {
            throw "Desktop executable was not created: $exe"
          }

      - name: Build installer
        shell: pwsh
        run: '& "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" "/DMyAppVersion=$env:APP_VERSION" installer\ClinicDataSystem.iss'

      - name: Verify installer exists
        shell: pwsh
        run: |
          $expected = "release\ClinicDataSystem-Setup-$env:APP_VERSION.exe"
          if (-not (Test-Path $expected)) {
            throw "Windows installer was not created: $expected"
          }
          Write-Host "Installer: $expected"

      - name: Upload installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: ClinicDataSystem-Windows-${{ env.APP_VERSION }}
          path: release/*.exe
          if-no-files-found: error
          retention-days: 14

      - name: Publish tagged release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: release/*.exe
          generate_release_notes: true
